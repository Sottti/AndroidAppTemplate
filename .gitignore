# Already in your file (keep these)
*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
# local.properties # (You had this twice, removed one)

# Project exclude paths (keep these if you like, but /build should cover them)
# /buildSrc/build/
# ... your other specific build paths ...

# --- Recommended Additions ---

# Generated binary files (add these)
*.apk
*.ap_
*.aab
*.dex
*.class

# More IDE files (add these)
/.idea/gradle.xml
/.idea/misc.xml
/.idea/runConfigurations/
/.idea/deploymentTargetDropDown.xml

# Gradle generated files (add .gradle/ if not covered by top-level rule)
# .gradle/ # Usually covered by the .gradle rule above

# Output directory (might be redundant if covered by /build)
# output/
