package com.sottti.android.app.template.data.items.datasource.remote

import com.sottti.android.app.template.model.Item

internal class ItemsRemoteDataSource() {
    fun getItems(page: Int, pageSize: Int): List<Item> {
        return emptyList()
    }
}
